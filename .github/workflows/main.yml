name: main

on:
  push:
    branches: [main]

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v2
      - name: Setup .NET
        uses: actions/setup-dotnet@v1
        with:
          dotnet-version: 6.0.x
      - name: Setup Node.js
        uses: actions/setup-node@v2
        with:
          node-version: "lts/*"
      - name: Test
        run: dotnet test -c Release
      - name: Publish GFK.Image locally
        run: dotnet publish GFK.Image -c Release -o pub/GFK.Image
      - name: Publish GFK.Image.cmd locally
        run: dotnet publish GFK.Image.cmd -c Release -o pub/GFK.Image/cmd
      - name: Test GFK.Image manifest
        shell: pwsh
        run: Test-ModuleManifest pub/GFK.Image/GFK.Image.psd1
      - name: Release
        env:
          GITHUB_TOKEN: ${{secrets.GITHUB_TOKEN}}
        run: npx semantic-release
      - name: Publish GFK.Image to PowerShell Gallery
        shell: pwsh
        run: |
          if ((Get-Module pub/GFK.Image -ListAvailable).Version -NE '1.0.0') {
            Publish-Module -Path pub/GFK.Image -NuGetApiKey ${{secrets.PSGALLERY_TOKEN}}
          }
